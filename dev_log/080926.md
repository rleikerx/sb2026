# 08/09/26

## Summary

Built a headless renderer and a rest-vs-posed contact sheet so creature pose work can
be checked by looking at it, then used them to fix the biped pose ladder.

**`tools/pose_render.py`** — a pure numpy z-buffer rasteriser over whatever
`AssetCatalog.assemble()` produces. No GPU, no Qt, no display, no new dependency:
numpy and Pillow only, both already required. It drives the real assembler rather
than reimplementing it. Orthographic side and front views, flat two-sided lambert,
a line marking the y=0 plane the skeleton root stands on, and parts tinted by limb
class with the left side drawn darker. The tint is a diagnostic, not decoration: in
flat grey an arm in the air and a leg in the air look identical, and a mirroring
fault is invisible without a side cue.

**`tools/pose_sheet.py`** — rest pose on the left, posed on the right, same camera
box for both, labelled with asset id, skeleton id and which rung of `stand_pose`
answered. Writes to `images/pose_review/`, which is tracked, so a pose change leaves
a picture somebody else can disagree with instead of a claim that it looked fine.

**Two defects fixed in `AssetManager.stand_pose`.**

*The upright gate was judging bipeds on a sixth of the evidence.* It probed frame 0
of each clip (`samples=1`) where every other rung probes six. Two rigs — Hungry Dead
(16) and Marsh Crone (49) — had frames passing the existing gate untouched and were
sitting at rest because nobody had looked past the first frame of their clips. A clip
does not put its calmest frame first.

*A biped that cannot straighten its legs had no rung at all.* `_stands_hunched`
allows leg tilt to 55° — but only alongside the three things a sprawl gets wrong:
feet still flat, body still taller than it is wide, feet still the lowest part of it.
Widening the tilt threshold on its own was already known to re-admit sprawl, and it
does; leg tilt alone cannot separate a crouch from a body lying down with its knees
up. Anything from 55° to 65° selects the same eight skeletons, so the bound sits in a
gap rather than on a cliff edge.

**Both are layered as fallbacks, so coverage cannot decrease**, and it did not:
nothing lost a pose it already had.

| layer | before | after |
|---|---|---|
| upright | 19 skeletons / 873 assets | 22 / 910 |
| hunched | — | 8 / 401 |
| grounded | 48 / 894 | 47 / 892 |
| rest | 18 / 613 | **8 / 177** |

**The first version of the hunched rung was wrong and the contact sheet is what
caught it** — 5 of 11 newly posed rigs were visibly bad: Grobones with its arms out,
Cave Troll mid-swing, Bearkin flung forward, Wight and Ice Fiend mid-stride. Both
causes were things the stance gates do not ask about. A walk frame answers every
question about legs and feet correctly, so `_feet_together` asks whether the feet are
a stride apart (measured: stands sit at 0.09–0.33 of body height, walks at
0.46–0.52). And `STAND_MAX_ARM_RISE` at +0.35 was measured on quadruped forelimbs,
where it means a settled dog; on two legs the same value is a figure reaching out in
front of itself, so `STAND_MAX_BIPED_ARM_RISE` is 0.0 — a standing biped does not
hold a forelimb above horizontal (measured: good poses −0.82 to −0.06, bad ones +0.35
to +0.58). Both are applied through `_stance_ladder`, three rungs strictest first,
because some rigs have frames with the arms down and none with the feet also
together, and one conjunction drops those rigs back to a swinging arm to buy a stride
they were never going to lose.

Second pass: **8 of 11 read correctly by eye**, up from 2.

`stand_pose` now records which rung answered (`stand_layer`), so the sheet reports
the choice rather than re-deriving it. A reporting tool that re-ran the gates itself
would drift the first time a rung moved, and the label would look right while being
wrong.

Invariants checked, not assumed: `--rest-pose` **byte-identical** on all six probe
ids (2000, 2001, 12000, 24000, 124100, 1000, sha256 compared before and after);
manifest **9924 rows across 6 kinds** after re-exporting all 2,380 creatures;
coverage never decreased.

## Current Work

Nothing in flight. The pose ladder and the two review tools are complete and the
creature export has been regenerated against them.

## Remaining Tasks

1. ~~**Hunting Griffon (skeleton 70) — needs a design decision, not a heuristic.**~~
   **Decided and done — the owner chose to author the wing rotations. See the
   second entry below.** The measurement in this item still stands and is what
   the fold was built against; what it got wrong is that the Aracoix and
   Nelchael are not splayed the same way the Griffon is.
   Measured every frame of all 7 clips: the 6 ground clips hold the wings at
   1.07–1.10 body-heights of lateral reach in *every* frame, and the only
   folded-wing frames belong to clip 70000076, which is a flight clip — rendered,
   they are a dive and a landing with the head near the ground, correctly rejected
   by the grounded test. Excluding wings from the calm tiebreak was tried and picks
   the same frame. So there is no frame that both stands on the ground and folds the
   wings, and frame selection cannot fix it. The options are to accept it as the
   creature (as Rat-Man and Ant Worker already are) or to author wing rotations,
   which changes the exporter from selecting source data to inventing it. Same
   family: Aracoix (18, 20) and Nelchael (117).

2. **Three of the eleven newly posed rigs still read wrong.** Wight (98) and Ice
   Fiend (131) get a stride because neither has a frame with the arms down *and* the
   feet together; Nelchael (117) has no arms-down frame at all. Each is a data limit
   rather than a threshold to tune.

3. **Eight skeletons / 177 assets remain at rest**, and they are the genuinely hard
   ones: 15, 34, 54, 67, 68, 91 have no flat-footed frame that is also taller than
   wide, and 100 and 109 parse no frames from any of their clips at all — that last
   pair is a motion-loading question, not a pose one.

4. **Never audited for assembly defects:** Structure 1057, Prop 1564, Item 4018,
   Deed 880, Other 25. `tools/pose_sheet.py --ids` renders any of them.

5. **Joint seams on every creature** (rigid one-mesh-per-bone, no vertex skinning).
   Large; needs scoping with the owner before anyone starts.

## Recommendations

- The renderer makes a per-kind sweep cheap — a contact sheet of every Structure at
  256px is a few minutes of CPU and would settle item 4 by inspection.
- `_feet_together` and `_arms_hang` are the first pose rules here whose thresholds
  were fitted to poses that had been *looked at* rather than to poses that scored
  well. Worth reusing that order — render, judge, then pick the number — for the
  wing question if the owner decides to author one.
- `tools/pose_render.py` deliberately draws no textures. If a future defect turns out
  to be a UV or material fault rather than a pose fault, that is the viewer's job,
  not this tool's.

---

# 08/09/26 — second run

## Summary

**This exporter now writes rotations of its own, and that is a line it had not
crossed before.** Every pose it has ever produced was *selected* — a frame the
client's own motion cache holds, picked by the stance ladder. The wing rotations
on four skeletons are **authored**: written here, by us, on the project owner's
explicit decision, because no frame in the cache supplies them.

**Exactly what is authored, so that nobody has to guess later:**

| | |
|---|---|
| skeletons | **18** and **20** (Aracoix), **70** (Hunting Griffon), **117** (Nelchael) |
| bones | the subtree under `LWINGJOINT`/`RWINGJOINT` (`LSHOULDERJB`/`RSHOULDERJ_B` on Nelchael), **excluding those root joints themselves** — 10 bones on 18/20/117, 12 on 70 |
| assets | **73 of the 2,380 exported creatures** |
| everything else | untouched: the body keeps the frame the ladder chose, and the root joints keep whatever that frame gave them |

`AssetManager.stand_layer` reports **`+wingfold`** for exactly these rigs, so the
contact sheets and every future report say out loud which poses are not wholly
the cache's. `13465`, `2002`, `2003` and `14078` are in `PROBE_ASSETS` for the
same reason — an authored pose is the one place a picture is the only evidence
there is.

**The rule is one sentence, and one constant.** *Every bone in the wing points
backward and `WING_FOLD_PITCH_DEG` (10 degrees) below horizontal.* Nothing else.

The **shape** is still the cache's: laying every wing bone along one direction is
what the source's own closed-wing frames do — the Griffon's clips rotate
`WING03/04/05` about X until all three point the same way. Only the *direction*
is ours.

**The direction is stated in model space, not in the torso's frame, and that is
the whole of why one constant serves three rigs.** Measured in the torso's frame
the Griffon wants 180 degrees (along its spine, which is horizontal) and the
Aracoix wants 120 or less (past that its wings go through the floor), and no
single value works. In model space those are the *same answer*, because a folded
wing lies back along the ground the creature stands on rather than along its own
spine. It rests on the rig facing +Z, which is this format's convention and was
checked rather than assumed: the torso's yaw in the chosen frame is **0.6 degrees
or less on all four rigs**.

**The 10 degrees was picked by rendering, not by scoring.** How far the wing tips
end up below the lowest part of the body, in body heights:

| pitch | 0 | 10 | 20 | 30 |
|---|---:|---:|---:|---:|
| Griffon (70) | +0.884 | **+0.333** | -0.203 | -0.705 |
| Aracoix (18) | +0.889 | **+0.665** | +0.447 | +0.243 |

so the Griffon puts its wings through the ground past about 15 degrees. At 0 the
*mesh* clears the crown of the back by so little that the wingtips read as two
spikes above the head — which the bone-tip figures do not show and a picture
does. 10 is the value where neither happens on any of the three.

## What it measures, before and after

Lateral reach is the previous entry's metric, reproduced exactly: the furthest
wing-bone tip from the centre line, in units of the body's own height measured
without the wings. Under it the six ground clips of skeleton 70 hold **1.072,
1.081, 1.072, 1.075, 1.076, 1.096** in their calmest frames — the 1.07-1.10 band
recorded above — and the flight clip dips to 0.498.

| skeleton | lateral reach | rise above the body | drawn height |
|---|---|---|---|
| 70 Hunting Griffon | **1.353 -> 0.279** | +0.594 -> -0.115 | 3.23 -> 3.03 |
| 18 Aracoix | 0.058 -> 0.058 | **+0.910 -> -0.110** | **8.40 -> 4.63** |
| 20 Aracoix | 0.066 -> 0.066 | **+1.106 -> -0.094** | **7.87 -> 3.94** |
| 117 Nelchael | **0.596 -> 0.202** | -0.080 -> -0.080 | 5.44 -> 5.30 |

Drawn width across the whole model falls **8.13 -> 1.97** on the Griffon and
**6.34 -> 2.53** on Nelchael.

**And that table corrects the premise this work started from.** "Winged creatures
export with their wings splayed" is true of the Griffon and of Nelchael, and is
*not* how the Aracoix was broken: its lateral reach was 0.058 before and 0.058
after, because its clips never touch its wing bones at all — the chosen frame
left them at rest, which on this rig is a three-bladed fan standing **straight up
over the head**. The figure was 8.40 units tall against a 4.06 body. Two
different defects, one fix.

## The mirror, and why no bone name is read for its side

A wing pair is one mesh reused per side (`flip`), so a rule that took the side
from a name prefix would be exactly the trap the brief warned about. Nothing here
reads a name for a side. Each bone is turned by the **shortest rotation from its
own rest direction onto the target**, and because the target lies in the mirror
plane, reflecting the argument reflects the answer — the two sides come out as
each other's mirror by construction rather than by a branch. The antipodal
fallback is a half turn about X, which is its own mirror, and X is always
perpendicular to the target because the fold direction has no lateral part.

Verified end to end rather than argued: **5 left and 5 right wing bones (3 and 3
on Nelchael) land on the fold direction to within 7e-16.** A mirror bug would
have put half of them somewhere else.

The drawn wings are *not* perfectly symmetric, and that is the body, not the
fold: mirror residual **1.79 -> 1.65 x10^-2** world units on skeleton 18 and
**1.95 -> 1.51** on Nelchael, whose torso is rolled 39 degrees so that both its
shoulders sit on the same side of the centre line. The fold **reduces** it on all
four.

## Selection was exhausted first, and it does not do the job

Before authoring anything: the previous entry proved no single frame both stands
on the ground and folds the wings, but it never tried taking the *wings* from one
frame and the *body* from another, which would still have been selection. So
every frame of every clip was tried as a wing donor onto the chosen body:

| skeleton | chosen | best graft | from |
|---|---:|---:|---|
| 70 | 1.353 | 0.553 | clip 70000055 frame 41 |
| 18 | 0.058 | 0.156 (worse) | 27 clips touch the wings |
| 20 | 0.066 | 0.182 (worse) | — |
| 117 | 0.596 | 0.204 | clip 117000076 frame 2 |

Rendered, the Griffon's best graft is a wing held **up**, not folded (rise 0.594
-> 0.975), and the Aracoix's best is a wing pointing **forward past its own
face**. Authoring is not a shortcut past a source that had the answer.

## Invariants, checked against a control rather than asserted

The control is the same code with `WING_FOLD_SKELETONS` emptied. It has to be
forced while the list is empty, because `stand_pose` is lazy and cached — asked
afterwards, the control folds too, and the first run of the check reported
"reach 0.058 -> 0.058" on every rig for that reason.

- **`--rest-pose` byte-identical, 10 of 10 ids** — the six this project fixes on
  (2000, 2001, 12000, 24000, 124100, 1000) plus the four rigs this change
  touches (2002, 2003, 13465, 14078), sha256 with the fold on and off.
- **99 out-of-scope skeletons unchanged** — every `stand_pose` and `stand_layer`
  compared against the control. 15 skeletons in this cache carry wing bones and
  only 4 are in scope; the Lesser Wyvern and the Bat still spread their wings,
  which the regenerated probe sheet shows.
- **Coverage 92 -> 92 skeletons with a pose.** Nothing lost one.
- **Manifest 9924 rows across 6 kinds**, unchanged per kind (Item 4018, Creature
  2380, Prop 1564, Structure 1057, Deed 880, Other 25) after re-exporting all
  2,380 creatures. Non-creature kinds were not rewritten at all.

## Pictures, and what reading them changed

`images/pose_review/wings-before.png` and `wings-after.png`, both drawn by
`tools/pose_sheet.py` with the fold off and on. Before: the Griffon lying flat
with its wings in a wide flat V spanning far past its body, and both Aracoix
standing under a green windmill taller than they are. After: the Griffon's wings
lie back along its body and read edge-on from the front, the Aracoix stand clean
with the wings swept back off the shoulders, and Nelchael's second pair of limbs
lie back instead of one being held over its head. `probe-after_*.png` are
regenerated with the three new rows.

**Two things were judged wrong by eye and caught by measuring**, both worth
recording because both looked convincing:

- The Aracoix's best source graft read as *"a wing swept elegantly back"* and is
  a wing pointing **forward over the face**. The rig faces +Z — settled by the
  `BEARD` mesh sitting at +0.23 to +0.27 in Z on both Aracoix — and the camera
  had reframed between rows, which moved the body in the frame and inverted the
  read.
- Nelchael's folded wings read as *rising* behind the shoulders. Measured, every
  wing mesh's farthest vertex sits at Z -0.96 to -1.00 from its own joint: they
  point backward, as the constant says.

A third: the first mirror check compared the two drawn wings for symmetry and
failed on all four rigs at about 10^-2. That was the check being wrong, not the
fold — a leaning body cannot have symmetric shoulders — which is why it is now a
comparison against the control rather than against zero.

## Current Work

Nothing in flight. The fold is landed, verified, and the creature export is
regenerated against it.

## Remaining Tasks

1. **Nobody has looked at any of this in a real viewer.** The evidence is a
   flat-shaded contact sheet. That is the standing weakness of every pose
   decision in this repo, and it is sharper here because these rotations are
   invented.

2. **One pitch serves three rigs, and the two ends of the table would each
   prefer a different one.** Measured above: the Griffon cannot go past about 15
   degrees without its wings entering the ground, and the Aracoix reads better
   with more droop, up to about 45, where its wings hang like a cloak instead of
   standing out behind. 10 is the value where nothing is broken on any of them,
   not the best value for any of them. If the owner wants it changed it is one
   constant, and the sink table is the cost.

3. **Nelchael's body is still wrong, and the fold does not touch it** — item 2 of
   the entry above stands: skeleton 117 has no frame with its arms down, so the
   ladder gives it a lean with one shoulder 39 degrees off level. Its wings are
   now folded onto a bad stance.

4. **Eleven other skeletons carry wing bones and are deliberately not folded**
   (15, 35, 44, 45, 46, 51, 57, 88, 89, 91, 115, 116 — the Lesser Wyvern and the
   Bat among them). Some of those are fliers where spread wings are correct.
   Extending the scope means looking at each, and is the owner's call, not a
   one-line edit to `WING_FOLD_SKELETONS`.

5. Items 2-5 of the entry above are otherwise unchanged: three biped rigs still
   read wrong, eight skeletons remain at rest, four asset kinds have never been
   audited, and the joint seams are untouched.

## Recommendations

- **Keep the `+wingfold` suffix load-bearing.** It is the only thing separating
  an authored pose from a selected one in any output this project produces, and
  the cheapest way to lose that distinction is a reporting tool that re-derives
  the layer itself instead of asking.
- The previous entry recommended *render, judge, then pick the number* for this
  work. It is what happened, and it changed the answer twice: the torso-frame
  formulation looked right in the numbers and was refuted by the Aracoix's
  pictures, and the pitch was chosen off a mesh overhang that no bone-tip metric
  reported.
- If the wing scope ever widens, the thing to measure first is not lateral reach
  but **drawn height** — that is what caught the Aracoix, whose lateral reach was
  correct all along.
